# Trevo Vault — iOS / App Store Connect
#
# Authentication is via the App Store Connect API key (see `asc_api_key` in the Fastfile),
# NOT an Apple ID. Deliberately no `apple_id` here: an Apple ID login re-introduces an
# interactive 2FA prompt on a departed employee's account. The API key resolves the
# App Store Connect team on its own.

app_identifier("finance.trevo.vault")
team_id("9NMWDV6M74") # TRAIT TECH PTE LTD — Apple Developer Portal team
